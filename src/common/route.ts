export class Route {

    dest = '';
    route= '';     // was 'gpxData'
    id = 0;
    climbing = 0;
    distance= 0;
    owner = '';
    //hasGPX = this.gpxData.length > 0;
    hasGPX = false;
    // not actually used but required for compatability with old DB
    description = '';

   
}